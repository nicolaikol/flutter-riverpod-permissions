name: riverpod_permissions
description: "A Flutter package for managing permissions with Riverpod."
version: 0.0.1
homepage:

environment:
  sdk: ^3.11.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_hooks: ^0.21.3+1
  flutter_riverpod: ^3.1.0
  freezed_annotation: ^3.0.0
  hooks_riverpod: ^3.3.1
  hugeicons: ^1.1.5
  logging: ^1.3.0
  permission_handler: ^12.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  freezed: ^3.2.5
  build_runner: ^2.12.2

flutter:
